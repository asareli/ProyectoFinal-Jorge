// ************************ DOM
console.log(document.body);
const contenido = document.getElementById("contenido") //acceder al elemento mediante ID
console.log(contenido);
const parrafos = document.getElementsByClassName("parrafo");

// ************************